# วิธีเปลี่ยนจาก SQLite เป็น MySQL

## ⚠️ สิ่งที่ต้องรู้
ตอนนี้โปรเจคใช้ **SQLite** (เก็บข้อมูลในไฟล์ `database/database.sqlite`) 
ไม่ใช่ MySQL นั่นคือเหตุผลที่หาไม่เจอใน phpMyAdmin

---

## 📋 ขั้นตอนการเปลี่ยนเป็น MySQL

### ขั้นตอนที่ 1: แก้ไขไฟล์ `.env`

เปิดไฟล์ `.env` ในโปรเจค และเปลี่ยนจาก:

```env
DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=
```

**เป็น:**

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=shop_project
DB_USERNAME=root
DB_PASSWORD=          # ใส่รหัสผ่าน MySQL ของคุณ (ถ้ามี)
```

**หมายเหตุ:** 
- `DB_DATABASE` = ชื่อ database ที่คุณต้องการ (เช่น `shop_project`)
- `DB_USERNAME` = username MySQL ของคุณ (ปกติคือ `root`)
- `DB_PASSWORD` = รหัสผ่าน MySQL ของคุณ (ถ้าไม่มีให้เว้นว่าง)

---

### ขั้นตอนที่ 2: สร้าง Database ใน MySQL

#### วิธีที่ 1: ใช้ phpMyAdmin
1. เปิด phpMyAdmin (`http://localhost/phpmyadmin`)
2. คลิกแท็บ **SQL**
3. พิมพ์คำสั่ง:
```sql
CREATE DATABASE shop_project CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```
4. คลิก **Go** หรือกด Enter

#### วิธีที่ 2: ใช้ MySQL Command Line
```bash
mysql -u root -p
```

จากนั้นรัน:
```sql
CREATE DATABASE shop_project CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

---

### ขั้นตอนที่ 3: รัน Migrations ใหม่

หลังจากแก้ไข `.env` แล้ว ให้รันคำสั่ง:

```bash
php artisan config:clear
php artisan migrate:fresh --seed
```

**คำเตือน:** `migrate:fresh` จะลบข้อมูลเก่าทั้งหมดและสร้างใหม่!

ถ้าไม่อยากลบข้อมูลเก่า ให้ใช้:
```bash
php artisan config:clear
php artisan migrate
```

---

### ขั้นตอนที่ 4: ตรวจสอบการเชื่อมต่อ

รันคำสั่งนี้เพื่อตรวจสอบ:
```bash
php artisan db:show
```

ควรเห็น:
```
MySQL ...................................................................................................... x.x.x
Connection .................................................................................................. mysql
Database ................................................................................................ shop_project
Host .................................................................................................. 127.0.0.1
Port ......................................................................................................... 3306
```

---

## ✅ ตรวจสอบข้อมูลใน MySQL

หลังจากเปลี่ยนเป็น MySQL แล้ว คุณสามารถเช็คข้อมูลได้:

### ผ่าน phpMyAdmin:
1. เปิด phpMyAdmin
2. เลือก database `shop_project` (ซ้ายมือ)
3. จะเห็นตารางทั้งหมด: `users`, `products`, `carts`, etc.
4. คลิกที่ตารางเพื่อดูข้อมูล

### ผ่าน Laravel Tinker:
```bash
php artisan tinker
```

```php
User::all();
Product::all();
Cart::all();
```

---

## 🔄 ถ้าต้องการย้ายข้อมูลจาก SQLite ไป MySQL

ถ้ามีข้อมูลใน SQLite และต้องการย้ายไป MySQL:

1. **Export ข้อมูลจาก SQLite:**
```bash
php artisan tinker
```

```php
// Export Users
$users = User::all();
file_put_contents('users.json', $users->toJson());

// Export Products
$products = Product::all();
file_put_contents('products.json', $products->toJson());

// Export Carts
$carts = Cart::all();
file_put_contents('carts.json', $carts->toJson());
exit;
```

2. **เปลี่ยนเป็น MySQL** (ตามขั้นตอนข้างบน)

3. **Import ข้อมูลกลับ:**
```bash
php artisan tinker
```

```php
// Import Users
$users = json_decode(file_get_contents('users.json'), true);
foreach($users as $user) {
    User::create($user);
}

// Import Products
$products = json_decode(file_get_contents('products.json'), true);
foreach($products as $product) {
    Product::create($product);
}

// Import Carts
$carts = json_decode(file_get_contents('carts.json'), true);
foreach($carts as $cart) {
    Cart::create($cart);
}
exit;
```

---

## ❓ ปัญหาที่พบบ่อย

### Error: Access denied for user
- ตรวจสอบ `DB_USERNAME` และ `DB_PASSWORD` ใน `.env`
- ตรวจสอบว่า MySQL Server กำลังทำงานอยู่

### Error: Unknown database 'shop_project'
- ตรวจสอบว่าได้สร้าง database แล้ว (ขั้นตอนที่ 2)
- ตรวจสอบชื่อ database ใน `.env` ตรงกับที่สร้าง

### Error: Table doesn't exist
- รัน `php artisan migrate` อีกครั้ง

---

## 📝 สรุป

1. ✅ แก้ไข `.env` → เปลี่ยน `DB_CONNECTION=mysql`
2. ✅ สร้าง database ใน MySQL
3. ✅ รัน `php artisan config:clear`
4. ✅ รัน `php artisan migrate:fresh --seed`
5. ✅ ตรวจสอบใน phpMyAdmin

หลังจากนี้คุณจะเห็น database `shop_project` ใน phpMyAdmin แล้ว! 🎉

